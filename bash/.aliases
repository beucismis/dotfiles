#!/bin/bash

# Listing
alias l="ls -la"
alias ld="ls -ld */"
alias ls="ls --color=auto"

# Shortcuts
alias g="git"
alias s="sudo"
alias c="clear"
alias rr="rm -rf"
alias untar="tar -zxvf "
alias reload="source $HOME/.bashrc"

# Defaults
alias df="df -H"
alias du="du -ch"
alias mkdir="mkdir -pv"
alias grep="grep --color=auto"
alias nano="micro"

# Package Managers
alias pacman="sudo pacman"
alias pip3-upgrade="python3 -m pip install --upgrade pip"

# Dotfiles
alias dotfiles-check="git -C $HOME/.dotfiles status"

# Test
alias camera-test="ffplay /dev/video0"
